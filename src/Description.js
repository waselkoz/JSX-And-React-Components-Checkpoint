import React from 'react';
import product from './product';

const Description = () => {
    return <p style={{ color: '#666', fontSize: '1.1rem' }}>{product.description}</p>;
};

export default Description;
